-- Canonical plugin version (C6). Read by health.lua/commands.lua to detect a
-- mismatch against the `bin/prompt-nvim` launcher, which carries its own
-- VERSION string and must be bumped in lockstep.
return { version = "0.2.1" }
